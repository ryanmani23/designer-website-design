.DS_Store
# Raw source photos (originals); the site uses the converted WebP under assets/
discontinued products/
# Build toolchain — dist/ is regenerated by `npm run build` (Cloudflare runs it on deploy)
node_modules/
dist/
